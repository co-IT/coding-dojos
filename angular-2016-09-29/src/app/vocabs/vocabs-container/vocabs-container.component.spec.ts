/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { VocabsContainerComponent } from './vocabs-container.component';

describe('Component: VocabsContainer', () => {
  it('should create an instance', () => {
    let component = new VocabsContainerComponent();
    expect(component).toBeTruthy();
  });
});
